# AWS Configuration
# Replace these with your actual AWS credentials
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-west-1
AWS_S3_BUCKET_NAME=your-vector-storage-bucket

# Vector Configuration
VECTOR_DIMENSION=384  # Default for sentence-transformers all-MiniLM-L6-v2
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the placeholder values with your actual AWS credentials
# 3. Create an S3 bucket and update AWS_S3_BUCKET_NAME
# 4. Ensure your AWS IAM user has s3:GetObject, s3:PutObject, and s3:ListBucket permissions